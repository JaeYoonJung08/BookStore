doctype html
html
  head
    title User Form
    style.
      body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        flex-direction: column;
      }
      .form-container {
        display: flex;
      }
      form {
        display: flex;
        flex-direction: column;
        align-items: center;
        margin: 0 20px;
      }
      input {
        margin-bottom: 10px;
        padding: 5px;
        width: 200px;
      }
  body
    h1 이 페이지는 로그인과 회원가입을 위한 페이지입니다.
    .form-container
      form(action="/user/signin" method="post")
        div
          input(type="text" name="userName" placeholder="Enter your name")
        div
          input(type="password" name="userPassword" placeholder="Enter your password")
        div
          button(type="submit") signin
          
      form(action="/user/signup" method="post")
        div
          input(type="text" name="userName" placeholder="Enter your name")
        div
          input(type="password" name="userPassword" placeholder="Enter your password")
        div
          button(type="submit") signup
      form(action="/user/session-data" method="get")
        div
          button(type="submit") chj
